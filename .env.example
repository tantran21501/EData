# Optional. Without this key, the wildfire collector is skipped.
# Create a free MAP_KEY at https://firms.modaps.eosdis.nasa.gov/api/map_key/
FIRMS_MAP_KEY=

# Optional tuning. Defaults are deliberately conservative.
HTTP_TIMEOUT_MS=30000
MAX_RETRIES=3
USER_AGENT=EarthLiveCamera-DataCollector/1.0
